import React, { Suspense } from 'react';
import Card from '../Card/Card';
// import { loadConfigFromFile } from 'vite';


const Apps = () => {

    return (
        <div>
            {/* <h1>hii</h1> */}
            {/* <Suspense fallback={load....}>
                
                {
                    data.map(singleApp)=> <Card key={singleApp.id}></Card>
                }
            </Suspense> */}
        </div>
    );
};

export default Apps;